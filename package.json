{
  "name": "coinflip-tg-admin",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://gamedevby.github.io/coinflip-tg-admin/",
  "dependencies": {
    "moment": "^2.30.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-scripts": "5.0.1",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build -r https://github.com/GameDevBy/coinflip-tg-admin.git",
    "deploy:dev": "PUBLIC_URL=/coinflip-tg-admin/dev npm run build && gh-pages -d build -b gh-pages -r https://github.com/GameDevBy/coinflip-tg-admin.git --dest dev",
    "eject": "react-scripts eject"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
